# Source

These locale headers are copied from https://github.com/joelspadin/zmk-locale-generator 

They allow to use an e.g. German keyboard layout in the OS and still have access to the modified symbols (like `@`, `!`).



